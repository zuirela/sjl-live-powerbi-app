<% layout('layout', { account: account, title: report.name }); %>

<h2><%= report.name %></h2>
<p>Tässä näkymässä esitetään Power BI -raportti, joka liittyy toimihenkilöryhmään <%= groupName %>. Upotuslinkit ja tunnisteet on määriteltävä sovelluksen konfiguraatiossa.</p>

<% if (report.embedUrl) { %>
  <!--
    Upotettu Power BI -raportti. Käytämme yksinkertaista iframe-elementtiä.
    Embed URL:n tulee sisältää tarvittavat parametrit (token tai muuta) jotta
    raportti voidaan esittää. Lisätietoja on README.md tiedostossa.
  -->
  <div class="report-container">
    <iframe
      title="Power BI Report"
      width="100%"
      height="700"
      src="<%= report.embedUrl %>"
      frameborder="0"
      allowFullScreen="true"
    ></iframe>
  </div>
<% } else { %>
  <p class="warning">Raportin upotusta varten ei ole määritelty URL-osoitetta. Määritä raportin embed URL config.js-tiedostossa tai ympäristömuuttujissa.</p>
<% } %>
